clc;
clear all;
d=input('Enter diameter in cm=');
r=d/2;
rad=r*10^(-2);
D=input('Enter distance between conductor in m=');
r1=rad*0.7788;
L=2*10^(-7)*log(D/r1);
C=(2*pi*8.854*10^(-12))/(log(D/rad));
fprintf('n\value of inductor=%f\n',L);
fprintf('|n value of capacitor=%f',C);